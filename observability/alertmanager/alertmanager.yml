global:
  resolve_timeout: 5m

route:
  receiver: local-webhook
  group_by:
    - alertname
    - severity
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://webhook-receiver:8080/
        send_resolved: true
